label rescue
	menu label ^Rescue mode
	kernel ${KERNEL}
	append ${VIDEO_MODE} initrd=${INITRD} rescue/enable=true rescue/enable=true mirror/http/hostname=snapshot.debian.org mirror/http/directory=/archive/debian/${SNAPSHOT_TS} mirror/country=manual mirror/http/proxy=http://anchorage.home:3128 --- quiet ${CONSOLE}
