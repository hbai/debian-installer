MEDIA_TYPE = netboot image

NETBOOT_DIR_TARGETS = $(TEMP_INITRD) $(TEMP_KERNEL)

TYPE = netboot/screen

TARGET = $(NETBOOT_DIR) $(NETBOOT_TAR) $(MINIISO)
EXTRANAME = netboot/screen/

MANIFEST-NETBOOT_DIR = "boot directory for tftp server (GNU/screen installer)"
MANIFEST-NETBOOT_TAR = "tarball of boot directory (GNU/screen installer)"
