MEDIA_TYPE = netboot image

NETBOOT_DIR_TARGETS := $(TEMP_INITRD) $(TEMP_KERNEL)

TYPE = netboot/screen

TARGET = $(NETBOOT_DIR) $(NETBOOT_TAR) $(MINIISO)
EXTRANAME = netboot/screen/

BOOT_SCREEN_DIR = boot-screens/

MANIFEST-NETBOOT_DIR = "netboot directory for tftp server (GNU/screen installer)"
MANIFEST-NETBOOT_TAR = "tarball of netboot directory (GNU/screen installer)"
MANIFEST-MINIISO = "tiny CD image that boots the netboot-screen installer"
