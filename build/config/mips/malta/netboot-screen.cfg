MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL)
TYPE = netboot/screen
EXTRANAME = $(TYPE)/

MANIFEST-INITRD = "initrd for the MIPS Malta board (GNU/screen installer)"
MANIFEST-KERNEL = "kernel image for the MIPS Malta board (GNU/screen installer)"
